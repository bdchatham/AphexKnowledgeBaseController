apiVersion: apps/v1
kind: Deployment
metadata:
  name: knowledgebase-controller
  namespace: platform-system
  labels:
    app: knowledgebase-controller
    app.kubernetes.io/name: knowledgebase-controller
    app.kubernetes.io/component: controller
    app.kubernetes.io/part-of: aphex-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: knowledgebase-controller
  template:
    metadata:
      labels:
        app: knowledgebase-controller
        app.kubernetes.io/name: knowledgebase-controller
        app.kubernetes.io/component: controller
        app.kubernetes.io/part-of: aphex-platform
    spec:
      serviceAccountName: knowledgebase-controller
      containers:
        - name: manager
          image: ghcr.io/bdchatham/knowledgebase-controller:latest
          args:
            - --leader-elect=true
            - --metrics-bind-address=:8080
            - --health-probe-bind-address=:8081
          ports:
            - name: metrics
              containerPort: 8080
              protocol: TCP
            - name: health
              containerPort: 8081
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 65532
            capabilities:
              drop:
                - ALL
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      terminationGracePeriodSeconds: 30
